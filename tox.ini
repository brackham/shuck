[tox]
env_list = py311, py312, py313, lint

[testenv]
package = wheel
wheel_build_env = .pkg
extras = dev
commands = pytest -m "not regression" {posargs}

[testenv:lint]
base_python = python3.11
skip_install = true
deps = ruff
commands =
    ruff check .
    ruff format --check .
