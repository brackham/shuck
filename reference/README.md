# Reference data

Full raw iSHELL and SpeXTool reference products are intentionally not tracked in Git.

Set `SHUCK_REFERENCE_DATA` to a local directory containing the regression dataset. See
`docs/VALIDATION_PLAN.md` for the suggested layout.
